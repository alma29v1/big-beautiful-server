# Campaign configuration
class CampaignConfig:
    def __init__(self):
        self.default_settings = {
            "enabled": True,
            "name": "Default Campaign"
        }

CAMPAIGN_CONFIG = {
    "default_settings": {
        "enabled": True,
        "name": "Default Campaign"
    }
}
